{
  "name": "@nesqbot/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Nesqual Tech design tokens and semantic colour roles for Nesq Bot",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "//": [
    "SOURCE-ONLY PACKAGE — see the note in packages/protocol/package.json.",
    "main/types/exports point at TypeScript source because Vite and Metro",
    "compile it directly; pointing at dist/ would break hot reload."
  ],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "assets"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "check": "node scripts/check-brand.mjs",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
