-r requirements.txt

# Test runner — pinned to match apps/worker/requirements-dev.txt
pytest==8.3.5
pytest-asyncio==0.25.3
anyio==4.9.0
# httpx (0.28.1, already pinned in requirements.txt) supplies ASGITransport.

# Lint / type check — configured in pyproject.toml
ruff==0.11.2
mypy==1.15.0
