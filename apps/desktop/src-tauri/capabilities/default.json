{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window, tray, deep-link and shell-open permissions for the Nesq Bot shell. There is deliberately no HTTP permission: Entra token redemption is a native command (src/entra.rs), not a frontend-issued request, so the webview has no way to make the shell fetch a URL.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "core:window:allow-close",
    "core:webview:default",
    "core:menu:default",
    "core:tray:default",
    "shell:allow-open",
    "deep-link:default"
  ]
}
